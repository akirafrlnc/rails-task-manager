<ul>
<% @tasks.each do |task| %>
	<li><%= link_to task.title, task_path(task.id) %></li>
	<li><%= task.details %></li>
	<li><%= task.completed %></li>
<% end %>
</ul>

<%= link_to "Add a new task", new_task_path%>
